{
  "name": "sml2r-node",
  "version": "1.0.3",
  "description": "A Node.js module for the Super Mario Land 2 Randomizer ",
  "main": "./dist/index.js",
  "files": [
    "dist/**/*"
  ],
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/* && tsc -p tsconfig.json && copyfiles -f src/patches/*.ips dist/patches",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/slashinfty/sml2r-node.git"
  },
  "keywords": [
    "randomizer"
  ],
  "author": "Matt Braddock",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.15.11",
    "copyfiles": "^2.4.1",
    "typescript": "^5.0.3"
  }
}
